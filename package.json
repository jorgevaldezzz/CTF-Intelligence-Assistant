{
  "name": "ctf-intelligence-assistant",
  "private": true,
  "type": "module",
  "scripts": {
    "ingest": "tsx ingestion/run.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
